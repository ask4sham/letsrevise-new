{
  "scripts": {
    "test": "jest",
    "validate:bundle": "node scripts/validate-lesson-bundle.js",
    "generate:bundle": "node scripts/generate-lesson-bundle.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "mongoose": "^9.1.0"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "jest": "^30.2.0",
    "mongodb-memory-server": "^11.0.1",
    "supertest": "^7.2.2"
  }
}

