# Reviews integration test

Run the review submission integration tests:

```bash
cd backend
npx jest tests/reviews.create.integration.test.js --no-coverage
```

(Note: `cd backend` and `npx jest ...` are two separate lines.)
