# Force LF for hash-locked statutory curriculum artifacts
docs/curriculum/statutory/*.json text eol=lf

# Track large PDFs with Git LFS (avoids pointer/stub in repo)
frontend/public/docs/*.pdf filter=lfs diff=lfs merge=lfs -text
