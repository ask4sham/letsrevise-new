/** Shared topic picker value for manual Create Lesson and AI generator. */
export type TopicSelectionValue = {
  subject: string;
  specKey: string;
  mainTopicTitle: string;
  topicKey: string;
  topic: string;
};
